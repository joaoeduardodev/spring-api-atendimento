server:
  error:
    include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/atendimento?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

